Found 5 call sites still using `strict=`. Here's the full picture:

**Broken callers (will silently ignore the kwarg and use the default `"strict"` mode):**

| File | Line | Old call | Fix needed |
|---|---|---|---|
| `src/analysis/aggregate.py` | 12 | `strict=False` | `mode="lenient"` |
| `src/preprocessing/normalize.py` | 13 | `strict=True` | `mode="strict"` (or drop — it's the default) |
| `src/pipeline/run.py` | 24 | `strict=True` | `mode="strict"` (or drop) |
| `tests/test_transform.py` | 9 | `strict=True` | `mode="strict"` |
| `tests/test_transform.py` | 14 | `strict=False` | `mode="lenient"` |

There's also a secondary issue: the docstring in `transform.py:16-17` still describes the old `strict: bool` parameter — it needs updating too.

**Documentation** (`docs/tutorial.md:13`, `README.md:11`) also uses `strict=True` but those won't cause runtime failures.

Want me to fix all of these?


--- STDERR ---
