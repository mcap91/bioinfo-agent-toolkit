All 11 tests pass.

**Two fixes were needed:**

1. **`src/core/transform.py`** — Renamed the `mode: str = "strict"` parameter to `strict: bool = True` and updated the condition from `mode == "strict"` to `strict`. Every caller in the codebase (tests, `aggregate.py`, `run.py`, `normalize.py`) was already using `strict=True/False`; the function signature was the odd one out.

2. **`pyproject.toml`** — Added `[tool.pytest.ini_options] pythonpath = ["src"]` so pytest can find the `core`, `pipeline`, etc. packages in the `src/` layout. The original test run must have had the package installed via `pip install -e .`; this config makes it work without installation.


--- STDERR ---
