Here's the full impact report. **Yes, the rename broke several things** — here's what needs to be fixed:

---

## Impact: Moderate | Breaking: YES | Risk: MEDIUM

### Broken call sites (immediate `TypeError` at runtime)

| File | Line | Old call | Fix |
|------|------|----------|-----|
| `src/analysis/aggregate.py` | 12 | `strict=False` | `mode="lenient"` |
| `src/preprocessing/normalize.py` | 13 | `strict=True` | `mode="strict"` |
| `src/pipeline/run.py` | 24 | `strict=True` | `mode="strict"` |
| `tests/test_transform.py` | 9 | `strict=True` | `mode="strict"` |
| `tests/test_transform.py` | 14 | `strict=False` | `mode="lenient"` |
| `tests/test_pipeline.py` | 10 | `strict=False` | `mode="lenient"` |

### Additional issues (not crashes, but wrong/stale)

- **`tests/conftest.py:6`** — fixture has `"strict": True` → update to `"mode": "strict"`
- **`src/core/transform.py` docstring (line 18)** — still describes `strict: bool`, needs updating to `mode: str`
- **`docs/tutorial.md:13`** — code example still shows `strict=True`
- **`docs/api-reference.md:10`** and **`docs/architecture.md`** — stale references

### Transitive blast radius (no direct fixes needed, but verify behavior)

`src/analysis/cluster.py`, `src/plotting/charts.py`, and `src/preprocessing/batch.py` depend on the depth-1 callers — they won't crash on their own, but will if their upstream callers remain broken.

---

Want me to go ahead and update all 6 broken call sites, the docstring, and the test fixture now?


--- STDERR ---
