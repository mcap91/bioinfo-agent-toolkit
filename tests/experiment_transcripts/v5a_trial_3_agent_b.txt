Clean. Here's a summary of every change made:

| File | Change |
|---|---|
| `src/core/transform.py` | Fixed docstring: `strict:` → `mode:` with updated description |
| `src/analysis/aggregate.py` | `strict=False` → `mode="lenient"` |
| `src/pipeline/run.py` | `strict=True` → `mode="strict"` |
| `src/preprocessing/normalize.py` | `strict=True` → `mode="strict"` |
| `tests/test_transform.py` | Both `strict=True`/`strict=False` calls updated |
| `tests/test_pipeline.py` | `strict=False` → `mode="lenient"` |
| `docs/tutorial.md` | Example and description updated |

One thing I intentionally left alone: `tests/conftest.py` has `"strict": True` inside a plain config dict (`SAMPLE_TRANSFORM_CONFIG`), not as a keyword arg to `apply_transform`. That dict key is unrelated to the function signature change, so it's safe as-is.


--- STDERR ---
